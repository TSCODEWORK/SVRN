# SVRN Python dependencies
# Install with: pip install -r requirements.txt
#
# Note: the menubar app is now a native Swift binary.
# rumps is no longer required.

# ZIM file reading (offline library engine)
libzim>=3.4.0,<4.0
